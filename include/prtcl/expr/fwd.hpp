#pragma once

#include <boost/yap/yap.hpp>

namespace prtcl::expr {

//template <typename> struct is_global_field;
//template <typename> struct is_uniform_field;
//template <typename> struct is_varying_field;

//template <typename> struct is_loop;

//template <boost::yap::expr_kind, typename> struct nl_stmt;
//template <boost::yap::expr_kind, typename> struct al_stmt;
//template <boost::yap::expr_kind, typename> struct pl_stmt;

} // namespace prtcl::expr
